function [ scores ] = matching_single_fragment( new_fragment_path, PAM_list)
% matching_single_fragment Take path to single fragment, process new image
% and do everything and save the scores to the folder with a *_scores.mat
% file where * is the name of the query fragment
%   PAM_list is a list of strings where each is the name of the PAM where
%   we would like to search. If it ends with a .jpg, the .jpg is removed 
    
end

